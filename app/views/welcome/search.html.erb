<!-- <h1>Twitter Join Date</h1>

<div >
  <%= form_tag("/search", method: "get") do %>
  <%= label_tag(:name, "Search for a Twitter user: @",) %>
  <%= text_field_tag(:name, nil, ) %>
  <%= submit_tag("Search") %>
  <% end %>      
</div> -->

<section id="home" data-stellar-background-ratio="0.5">
    <div class="parallax-overlay"></div> 
      <div class="home-content text-center">
        <div class="jumbotron">            
            <div class="main-flex-slider">
                  <ul class="slides">
                    <li>
                      <div class="hero-unit">
                        <h1>Twitter Join Date</h1>
                        <h3> Use Twitter Join Date to find when a user joined Twitter</h3>
                      </li>
                    </ul>
                  </div> 
                <div class="home-arrow-down text-center">
            			  <%= form_tag("/search", method: "get") do %>
            			  <%= label_tag(:name, "Search for a Twitter user: @",) %>
            			  <%= text_field_tag(:name, nil, ) %>
            			  <%= submit_tag("Search") %>
            			  <% end %>      
      			   </div>
            </div>
        </div>
      </div>
    </div>
</section



